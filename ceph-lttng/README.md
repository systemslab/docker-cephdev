Builds an image with Ceph compiled with LTTnG. The image also has LTTnG tools
for starting, stopping, and viewing the tracepoints.

===================================================
Quickstart
===================================================

Build Ceph with LTTnG and put it in an image:

```bash
CEPH_SRC=~/ceph ./build
```
